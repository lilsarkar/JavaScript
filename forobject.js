const myObj={
    js:'javascript',
    cpp:'c++',
    rb:"ruby",
    switch:"sw"
    

}
for (const key in myObj) {
    console.log(`${key} shortcutis for ${myObj[key]}`)    

    }

    let pyhon=['saad','baat','kar']
    const pr=["java",'cpp',pyhon]
    {
        for (const key in pr) {

            console.log(pr[key])//accesing cvalue
        
        }
    }

//object is only iterator in for in loop and also u cann acces the key but for the value u have todeclare object and key in sam